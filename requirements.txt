# Core Numerical & Computer Vision Dependencies
numpy>=1.24.0
opencv-python>=4.8.0
scipy>=1.10.0
matplotlib>=3.7.0
Pillow>=9.5.0

# Deep Learning Framework (BézierLaneNet)
torch>=2.0.0
torchvision>=0.15.0

# Utilities & Control Theory
tqdm>=4.65.0
PyYAML>=6.0
control>=0.9.0
tensorboardX>=2.6

# Note: ROS 2 dependencies (rclpy, sensor_msgs, geometry_msgs, nav_msgs, std_msgs, cv_bridge)
# are provided by your ROS 2 environment (ROS 2 Jazzy or Humble recommended).
# On Ubuntu / Debian systems with ROS 2 installed, ensure cv_bridge is installed via:
# sudo apt-get install ros-$ROS_DISTRO-cv-bridge ros-$ROS_DISTRO-vision-opencv
